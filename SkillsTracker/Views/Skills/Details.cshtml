@model SkillsTracker.Models.Skill

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Skill</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.skill1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.skill1)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.link)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.link)
        </dd>

        @if (Model.ParentSkill.Count > 0)
        {
            <dt>
                @Html.DisplayNameFor(model => model.ParentSkill)
            </dt>
        }
        @foreach (var ps in Model.ParentSkill)
        {
            <dd>
                @Html.DisplayFor(modelItem => ps.skill1)
            </dd>
        }
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
